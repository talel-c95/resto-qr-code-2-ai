/**
 * Customer-facing routes.
 *
 * /                     → LandingPage
 * /guest                → GuestPage
 * /login                → LoginPage
 * /register             → RegisterPage
 * /scan                 → QRScanPage
 * /menu/:tableId        → MenuPage
 * /cart                 → CartPage
 * /checkout             → CheckoutPage
 * /orders/:id           → OrderTrackingPage
 * /receipt/:id          → ReceiptPage
 * /history              → OrderHistoryPage
 */

export {};
